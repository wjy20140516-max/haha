#pragma once
#include <string>

// 将变量转为字符串,例：ToStr(vih)等同于字符串"vih"
#define ToStr(name) (#name)

// shmoo全局变量
extern double vih;
extern double vil;
extern double vol;
extern double voh;
extern double period;
extern double edge_a1;
extern double edge_a2;
extern double edge_b1;
extern double vforce;
extern double strb;
